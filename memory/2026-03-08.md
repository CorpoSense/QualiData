# 2026-03-08 Memory

## User Management Implementation

### Completed Features
- User CRUD API endpoints (backend)
- User management UI page (/admin/users)
- Profile page (/profile)
- Role-based access control (admin, manager, user)
- Integration tests with SQLite in-memory DB

### Key Fixes
1. **Route ordering**: `/users/me` must come BEFORE `/users/{user_id}` in FastAPI
2. **Role enum**: Database uses `userrole` enum (admin, manager, user)
3. **Timezone field**: Added to User model - was missing
4. **TypeScript declarations**: Created `env.d.ts` for JS modules
5. **BootstrapVueNext**: Use unplugin-vue-components with BootstrapVueNextResolver

### Tech Stack
- Backend: FastAPI + SQLAlchemy + PostgreSQL
- Frontend: Vue 3 + Vite + BootstrapVueNext + Bootstrap 5
- Package manager: pnpm
- Tests: pytest + SQLite in-memory

### Important Files
- User model: `backend/app/db/models/user.py`
- Users router: `backend/app/routers/users.py`
- Frontend Users: `src/views/admin/Users.vue`
- Frontend Profile: `src/views/Profile.vue`
- Type declarations: `src/env.d.ts`
- BootstrapVueNext cheat sheet: `/root/.openclaw/workspace/bootstrap-vue-next-cheatsheet.md`

### Tests
- 132 tests passing (including 14 new integration tests)
- Integration tests use SQLite in-memory for real DB testing

### Notes
- Role values in DB: 'admin', 'manager', 'user' (lowercase)
- First registered user becomes admin by default
- Admin created from ADMIN_USER/ADMIN_PASSWORD env vars on startup
